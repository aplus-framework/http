version: "3"
services:
  package:
    image: registry.gitlab.com/aplus-framework/images/package:1
    container_name: package-http
    working_dir: /package
    volumes:
      - .:/package
    tty: true
